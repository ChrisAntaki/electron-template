{
  "name": "hello-world",
  "private": true,
  "scripts": {
    "start": "yarn electron --dev",
    "electron": "electron .",
    "package": "electron-packager --overwrite --icon=images/logo.png ."
  },
  "devDependencies": {
    "electron": "^11.2.1",
    "electron-packager": "^15.2.0",
    "electron-reload": "^1.5.0"
  },
  "dependencies": {
    "localforage": "^1.9.0"
  }
}
